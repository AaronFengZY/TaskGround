# TaskGround — Project Page

Placeholder project page for **TaskGround: Structured Executable Task Inference for
Full-Scene Household Reasoning**.

> 🚧 The full project page is under construction. The current `index.html` is a
> "Coming Soon" placeholder modeled after our prior project pages.

## Local preview

Open `index.html` directly in a browser, or serve the folder:

```powershell
# from this directory
python -m http.server 8000
# then visit http://localhost:8000
```

## Deploy via GitHub Pages

1. Push this folder to a GitHub repository (e.g. `TaskGround-Webpage`).
2. In **Settings → Pages**, set the source to the `main` branch / root.
3. The site will be available at `https://<user>.github.io/<repo>/`.

## Related project

- Code repo: <https://github.com/AaronFengZY/TaskGround>
